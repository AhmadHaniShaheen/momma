import 'package:flutter/material.dart';

class BootsScreen extends StatefulWidget {
  const BootsScreen({Key? key}) : super(key: key);

  @override
  State<BootsScreen> createState() => _BootsScreenState();
}

class _BootsScreenState extends State<BootsScreen> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.black,
      ),
    );
  }
}
